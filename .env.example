# Runtime paths
WORKFOLDER=./workfolder
MODEL_CACHE_DIR=./data/modelscope
CORS_ALLOW_ORIGINS=
CORS_ALLOW_ORIGIN_REGEX=

# Device: cuda, cuda:0, cpu, or auto
DEVICE=cuda

# TorchAudio 2.9+ defaults to TorchCodec; keep 0 to use FFmpeg/soundfile instead
USE_TORCHCODEC=0
# Optional explicit path when ffmpeg is not on PATH (Windows winget install)
# FFMPEG_PATH=C:\path\to\ffmpeg.exe

# OpenAI-compatible translator
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# FunASR
FUNASR_MODEL=iic/SenseVoiceSmall
FUNASR_VAD_MODEL=fsmn-vad

# VoxCPM
VOXCPM_MODEL=OpenBMB/VoxCPM2
VOXCPM_MODEL_DIR=
VOXCPM_OPTIMIZE=false
VOXCPM_LOAD_DENOISER=false
VOXCPM_CFG_VALUE=2.0
VOXCPM_INFERENCE_TIMESTEPS=10
VOXCPM_MIN_REFERENCE_MS=1200

# Optional proxy for yt-dlp
HTTP_PROXY=
