# Optional LiteLLM transport for the translation stage.
#
# Install alongside requirements.txt to route translation through LiteLLM
# instead of the OpenAI SDK, unlocking 100+ providers (Anthropic, Gemini,
# AWS Bedrock, Google Vertex AI, Azure, Groq, ...) via a single interface.
# Enable it with OPENAI_USE_LITELLM=1 (or the settings API `use_litellm` flag).
#
#   pip install -r requirements-litellm.txt
#
litellm>=1.89.0,<2.0.0
